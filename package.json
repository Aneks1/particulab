{
  "name": "particulab",
  "version": "0.0.0",
  "description": "Monorepo for particulab",
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev --parallel",
    "test": "turbo run test",
    "lint": "turbo run lint",
    "changeset": "changeset",
    "version-packages": "changeset version",
    "release": "turbo run build --filter=!./apps/website && changeset publish"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "packageManager": "pnpm@10.9.0",
  "devDependencies": {
    "@changesets/cli": "^2.29.2",
    "turbo": "^2.5.0",
    "typescript": "^5.3.3"
  }
}
